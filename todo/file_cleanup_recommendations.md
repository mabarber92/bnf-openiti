# File Cleanup Recommendations

## 1. Move to archive/ (untracked root-level files)

These files were created during the normaliser-fix debugging sprint and are now redundant. Move to `archive/debug_scripts/`:

| File | Reason |
|------|--------|
| `debug_ibnhajar_creator.py` | Single-record debug; superseded by `matching/scripts/debug_author_scores.py` |
| `debug_unicode_decomposition.py` | One-off normaliser diagnostic |
| `test_normalize_behavior.py` | Normaliser unit test written inline during debug |
| `test_normalize_real_bnf.py` | Same |
| `test_openiti_parameter.py` | Same |
| `validate_normalization_strategies.py` | Ad-hoc normalisation comparison |
| `validate_pipeline_wiring.py` | End-to-end wiring check; pipeline now validated via validate_correspondences_only.py |
| `validate_real_data.py` | Superseded |

Move to `archive/analysis_reports/`:

| File | Reason |
|------|--------|
| `AUTHOR_MATCHING_ANALYSIS.md` | Historical analysis; predates two-tier IDF design |
| `CREATOR_FIELD_ANALYSIS.md` | Historical analysis of creator field reweighting |
| `NORMALIZER_FIX_ANALYSIS.md` | Already exists in archive/ — duplicate |

---

## 2. Delete (data_samplers/ CSVs — not committed, regenerable)

These CSVs are outputs of debug/validation scripts and are gitignored or should be. They can be regenerated by running the relevant script:

| File | Regenerated by |
|------|---------------|
| `data_samplers/combined_scores_with_author.csv` | `matching/scripts/export_combined_scores.py` |
| `data_samplers/full_matching_scores.csv` | `matching/scripts/export_combined_scores.py` |
| `data_samplers/stage1_author_scores.csv` | `matching/scripts/export_author_scores.py` |
| `data_samplers/validation_matches.csv` | `matching/scripts/validate_correspondences_only.py` |

Add these patterns to `.gitignore` if not already there:
```
data_samplers/*.csv
```

---

## 3. Delete or rewrite (stale test)

| File | Issue | Recommendation |
|------|-------|---------------|
| `tests/test_confidence_filtering.py` | References removed `use_confidence_filtering` parameter; test will error | Delete — confidence filtering was removed as a design decision, not a bug |

---

## 4. Consolidate config templates

Three near-duplicate template files exist in the root:

| File | Contents |
|------|---------|
| `config.example.yml` | Full template with all options documented (referenced in PIPELINE.md Setup) |
| `config_template.yml` | Short template with only required paths |
| `openiti.yml` | OpenITI-specific config (corpus version tracking) |

**Recommendation:** Keep `config.example.yml` (it's the one PIPELINE.md points to) and delete `config_template.yml`. `openiti.yml` serves a separate purpose and should stay.

---

## 5. Review matching/scripts/ — possible consolidation

| Script | Status | Notes |
|--------|--------|-------|
| `validate_correspondences_only.py` | **Current** — primary validation |
| `validate_full_correspondences.py` | Review — may duplicate above |
| `validate_correspondences_with_thresholding.py` | Current — threshold sweep tool |
| `validate_500_sample.py` | Current — sample validation |
| `validate_author_matching.py` | Current — Stage 1 standalone |
| `parameter_sweep.py` | Stale references — uses old config parameter names (`TOKEN_IDF_PENALTY_EXPONENT`); needs updating before use |
| `export_author_scores.py` | Current |
| `export_combined_scores.py` | Current |
| `debug_author_scores.py` | Current |
| `debug_combined_scores.py` | Current |

**Recommendation:** Check whether `validate_full_correspondences.py` duplicates `validate_correspondences_only.py`; if so, delete it. Update `parameter_sweep.py` to use current config parameter names before relying on it.

---

## 6. matching/archive/ — already archived

`matching/archive/` contains historical fuzzy matching implementations and old tests. These are properly archived and do not need action.

---

## 7. parameter_optimization/ — stale references

`parameter_optimization/sweep_thresholds.py` references `TOKEN_IDF_PENALTY_EXPONENT` which does not exist in the current config. Do not run this script without updating it first.

---

## Summary

| Action | Count | Priority |
|--------|-------|---------|
| Move root debug files to archive/ | 8 files | Low |
| Move root analysis .md to archive/ | 2 files | Low |
| Add data_samplers/*.csv to .gitignore | 1 line | Medium |
| Delete stale test | 1 file | Medium |
| Delete duplicate config template | 1 file | Low |
| Review/consolidate validate scripts | 1-2 files | Low |
| Update parameter_sweep.py | 1 file | Before next param sweep |
