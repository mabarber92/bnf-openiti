# Local config — contains absolute paths, never commit
config.yml

# Pipeline outputs — large data files, intermediate stores, run artifacts
outputs/

# Validation/debug CSV outputs — regenerable from matching/scripts/
data_samplers/*.csv

# Ad-hoc survey JSON files generated at project root during development
full_survey*.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/

# OS
.DS_Store
Thumbs.db
